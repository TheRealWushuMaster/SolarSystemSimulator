# Legacy Tkinter app

The original Solara UI, built on `customtkinter`. Superseded by `app_ursina/`
(desktop) and `server/` + `web/` (browser) — untouched since Dec 2023, kept
here for nostalgia's sake rather than actively developed.

Run it with:

```
uv run python -m legacy
```
